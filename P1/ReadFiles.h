//
// Created by pedro on 09/03/2023.
//

#ifndef DA_TP_CLASSES_READFILES_H
#define DA_TP_CLASSES_READFILES_H


class ReadFiles {
public:
    /*!
 * reads information from the file and stores it in the Graph object
 * @param file filepath to fetch information from
 * @param graph Uni object that will store the information
 *//*
    static void readfile1(const string& file,Graph *graph);
    *//*!
 * reads information from the file and stores it in the Graph object
 * @param file filepath to fetch information from
 * @param graph Uni object that will store the information
 *//*
    static void readfile2(const string& file, Graph *graph);
    *//*!
 * reads information from the file and stores it in the Uni object
 * @param file filepath to fetch information from
 * @param graph object that will store the information
 *//*
    static void readfile3(const string& file, Graph *graph);*/
};


#endif //DA_TP_CLASSES_READFILES_H
